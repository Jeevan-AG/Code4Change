# Person 2 — set these (or ask Person 1 to add them to the shared project .env)

# Nemotron video/image analysis (Person 2 only)
NVIDIA_API_KEY=

# QR links on generated PDF certificates
APP_URL=https://kaushalpass.in

# Optional overrides (defaults: documents, certificates)
# STORAGE_BUCKET_DOCUMENTS=documents
# STORAGE_BUCKET_CERTIFICATES=certificates
# PASSPORT_CODE_PREFIX=KP-2025

# --- Shared backend (Person 1 / team root .env — NOT Person 2-owned) ---
# Person 2 code still READS these at runtime; Person 1 provisions Supabase + auth.
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
# SUPABASE_JWT_SECRET=
